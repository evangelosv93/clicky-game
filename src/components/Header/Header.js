import React from "react";
import "./Header.css";

const Header = props => (
  <header className="header">
    <h1>Random clicks</h1>
    <h2>
      Click on an image to earn points, lets see if this works!
    </h2>
  </header>
);

export default Header;
